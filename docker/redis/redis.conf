# Redis 配置
# 绑定地址
bind 0.0.0.0

# 端口
port 6379

# 密码（生产环境启用）
# requirepass your_redis_password

# 持久化
appendonly yes
appendfilename "appendonly.aof"

# 内存策略
maxmemory 256mb
maxmemory-policy allkeys-lru

# 日志
loglevel notice
